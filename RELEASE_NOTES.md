# ClassLens AI v1.0.3 / 课镜 AI

- 版本：v1.0.3（versionCode 32）
- 大小：9,971,712 字节（9.5 MB）
- 构建：R8 全量混淆 + 资源收缩（字典混淆 / 打散包结构 / 抹除源文件名）

## 本轮改动

1. **周课表表头吸顶 + 节次列吸左**：周一~周日表头始终固定在顶部，左侧第几节/上课时间列始终固定在左边；中间网格双向自由滚动，三块严格同步。节次列与表头收窄，屏显空间更大；上课时间跟随主体配色（换配色方案自动同色）。
2. **拖动手感彻底修复（官方管线）**：速度采样补齐历史采样点、惯性初速度参数修正（修掉松手瞬移/快滑砸边界）、系统级最小惯性速度阈值（慢拖稳停不漂移）、亚像素位移累加（高刷屏不再一跳一跳）。
3. **AI 对话体验**：切换标签页不再跳回顶部（记住上次滚动位置），新增「回到底部」悬浮按钮；上下文用量小圈可点击查看明细（已用 tokens / 模型上限 / 剩余百分比）。
4. **自定义 AI 接口按协议组织**：新增「接口协议」下拉（Anthropic Messages /v1/messages、Chat Completions /chat/completions、Responses /responses），选中即自动填默认地址；新增 OpenAI Responses 协议完整实现（工具调用 + 图片输入）。
5. **课程门数口径修正**：课名 + 上课教师相同的多个时段按一门课计；总学分按门去重累加。今日页、AI 页标题、AI 统计全部同步该口径。
6. **暗色模式可读性**：全局灰色文字提亮（副标题、节次时间等），暗色下不再发暗；测试按钮、教务系统搜索/刷新等图标暗色下改为白色。
7. **AI 气泡**：底色加深、更清晰；「AI 气泡不透明度」滑条只作用于气泡背景，文字始终保持清晰。

# ClassLens AI v1.0.2 / 课镜 AI

- 版本：v1.0.2（versionCode 31）
- 大小：10,173,483 字节（9.7 MB）

## 本轮改动

1. **周课表拖动手感升级**：松手惯性改用 Android 原生 spline 阻尼物理（与系统列表一致），滑动丝滑过渡、精准停在边界，不再"一划就停"。
2. **AI 助手全接口开放 + 安全护栏**：
   - 新增课表管理（新建/切换/重命名/删除，删除激活课表自动拒绝）、外观设置（主题/配色/文字大小/玻璃模糊等，字段级合并+数值钳制，背景图与语言不可被 AI 修改）。
   - 破坏性操作（清空/删除/导入覆盖）前自动快照，支持一键"撤销上次操作"回滚。
   - 所有写入字段做范围钳制（节次恒为 start≤end、星期 1-7 等），AI 无法写出坏数据。
3. **更聪明的任务处理（类 CC/ZCode）**：今天课表 / 明天 / 下节课 / 空闲节次 / 第几周 / 统计 / 冲突等确定性提问走本地快速通道，秒回、零流量、没配 API key 也能用；复杂任务（批量/整表/分析类）自动先列步骤计划再逐项执行，每步写入结果带回写后状态供自校验。
4. **全新液态玻璃 App 图标**：桌面、设置等所有系统图标位同步更换。
